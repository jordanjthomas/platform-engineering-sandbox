# Test manifest: should be REJECTED by require-resource-limits policy
apiVersion: v1
kind: Pod
metadata:
  name: test-no-resources
  namespace: platform
spec:
  securityContext:
    runAsNonRoot: true
    runAsUser: 1000
  containers:
    - name: test
      image: busybox:1.36
      command: ["sh", "-c", "echo hello"]
