apiVersion: v1
kind: Secret
metadata:
  name: namespace-provisioner-token
  namespace: platform
  labels:
    app.kubernetes.io/name: namespace-provisioner
    app.kubernetes.io/component: auth
  annotations:
    description: "Placeholder token — replaced by External Secrets Operator in Phase 3"
type: Opaque
stringData:
  ADMIN_TOKEN: "placeholder-change-me"
